{
  "buildCommand": "cd Outing && npm install && npm run build",
  "outputDirectory": "Outing/dist",
  "installCommand": "cd Outing && npm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}